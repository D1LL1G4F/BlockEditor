/*#include <iostream>
#include "src/block.h"

using namespace std;

int main(int argc, char const *argv[]) {
  Block blck("SUM",1,1);
  blck.setPort(Block::PORT_IN,0,10);
  if (blck.areInPortsSet()) {
    double val = blck.getInPort(0)->getValue();
    cout << val << "\n";
  }
  return 0;
}
*/
